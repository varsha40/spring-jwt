#configure database
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/loginapp?serverTimeZone=UTC
    username: root
    password: varsha2000
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    show-sql: true